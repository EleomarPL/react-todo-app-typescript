import styled from 'styled-components'

const SectionList = () => {
  return (
    <Section>
      SectionList
    </Section>
  )
}

const Section = styled.section`
  text-align: center;
`

export default SectionList
